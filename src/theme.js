import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      light: '#33BCCD',
      main: '#00ACC1',
      dark: '#007887'
    },
    secondary: {
      light: '#CD4333',
      main: '#C11500',
      dark: '#870e00'
    },
  },
});

export default theme;